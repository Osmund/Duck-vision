# Duck-Vision - Raspberry Pi 5 AI Camera System
# Python 3.11+ | Raspberry Pi OS Bookworm 64-bit
#
# VIKTIG: picamera2 og libcamera installeres via system-pakker (apt),
# IKKE via pip. Se setup.sh for komplett installasjon.
#
# System-pakker som må installeres først:
#   sudo apt install -y python3-libcamera python3-picamera2 python3-opencv
#   sudo apt install -y imx500-all
#   sudo apt install -y libportaudio2 portaudio19-dev libcap-dev
#   sudo apt install -y cmake build-essential libopenblas-dev liblapack-dev

# MQTT Communication
paho-mqtt==2.1.0

# Image Processing
# opencv-python installeres via apt (python3-opencv) for IMX500-kompatibilitet
Pillow==10.2.0

# Face Recognition
face-recognition==1.3.0
dlib>=19.24  # 20.0.0 fungerer fint på Pi 5

# Object Detection
ultralytics==8.1.24  # YOLOv8
torch==2.1.2
torchvision==0.16.2

# Utilities
python-dotenv==1.0.0
numpy==1.26.3

# Speaker / Voice Recognition
sounddevice>=0.5.0
resemblyzer>=0.1.4
webrtcvad>=2.0.10
soxr>=0.3.2
librosa>=0.9.1

# Singing Duck (TTS + sangskriving)
openai>=1.0
azure-cognitiveservices-speech>=1.37
pyworld>=0.3.4
soundfile>=0.12
midiutil>=1.2.1
mido>=1.3
